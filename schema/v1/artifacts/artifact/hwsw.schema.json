{
    "$schema": "https://raw.githubusercontent.com/alrosado/azure-rmf-accelerator-json/refs/heads/main/schema/v1/artifacts/artifactContent.schema.json",
    "$id": "https://raw.githubusercontent.com/alrosado/azure-rmf-accelerator-json/refs/heads/main/schema/v1/artifacts/artifact/hwsw.schema.json",
    "title": "Hardware and Software (HWSW) Schema",
    "$comment": "1.0.0",
    "description": "Schema for defining and generating System Design Specification Documents that comprehensively describe solution architecture, requirements, security controls, and operational considerations for Azure-based systems.",
    "type": "object",
    "properties": {
        "$schema": {
            "type": "string",
            "description": "The schema to verify this document against.",
            "format": "uri-reference"
        },
        "$id": {
            "type": "string",
            "description": "A unique identifier for the schema."
        },
        "$comment": {
            "type": "string",
            "description": "Comments from schema authors to readers or maintainers of the schema."
        },
        "title": {
            "type": "string",
            "description": "The title of the artifact when generated."
        },
        "artifactType": {
            "type": "string",
            "description": "The type of the artifact to be generated.",
            "enum": [
                "hwsw"
            ]
        },
        "outputType": {
            "type": "string",
            "description": "The output type of the artifact when generated.",
            "enum": [
                "csv"
            ]
        },
        "order": {
            "type": "array",
            "description": "The order of data properties when generated.",
            "uniqueItems": true,
            "items": {
                "type": "string",
                "minLength": 1,
                "enum": [
                    "Asset Type",
                    "Asset",
                    "STIG",
                    "STIG Applyer",
                    "Purpose"
                ]
            }
        },
        "data": {
            "type": "array",
            "items": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                    "Asset Type": {
                        "type": "string",
                        "description": "The type of asset. Software, library, or resource type friendly name."
                    },
                    "Asset": {
                        "type": "string",
                        "description": "The name or identifier of the asset."
                    },
                    "STIG": {
                        "type": "string",
                        "description": "The STIG associated with the asset. Blank if there is no associated STIG."
                    },
                    "STIG Applyer": {
                        "type": "string",
                        "description": "The role responsible for applying the STIG to the asset."
                    },
                    "Purpose": {
                        "type": "string",
                        "description": "The purpose of the asset within the system."
                    }
                },
                "required": [
                    "Asset Type",
                    "Asset",
                    "Purpose"
                ]
            },
            "minItems": 0,
            "uniqueItems": true,
            "description": "Data to use for artifact generation."
        }
    },
    "additionalProperties": false,
    "required": [
        "$schema",
        "title",
        "outputType",
        "data"
    ]
}